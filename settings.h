#pragma once

namespace Settings
{
	static uint64_t bypassAddress = 0x16480; //0x16350
	static uint64_t dlcmountcount = 0x69; // Maybe if it gives too many errors we should write to the offset manually :)
}